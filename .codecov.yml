coverage:
  status:
    project:
      language-server:
        flags:
          - language-server
      sobject-metadata:
        flags:
          - sobject-metadata
      soql-builder-ui:
        flags:
          - soql-builder-ui
      soql-model:
        flags:
          - soql-model
flags:
  language-server:
    paths:
      - packages/language-server
  sobject-metadata:
    paths:
      - packages/sobject-metadata
  soql-builder-ui:
    paths:
      - packages/soql-builder-ui
  soql-model:
    paths:
      - packages/soql-model
